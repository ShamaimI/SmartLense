# ── Core CV & ML ──────────────────────────────────────────────────────────────
streamlit>=1.32.0
opencv-python>=4.9.0
numpy>=1.24.0
Pillow>=10.0.0

# ── PyTorch (CPU; replace with CUDA build for GPU) ─────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── Object Detection & Tracking ───────────────────────────────────────────────
ultralytics>=8.0.0           # YOLOv8 + ByteTrack (built-in via model.track())

# ── CLIP (text-image matching + Re-ID embeddings) ─────────────────────────────
git+https://github.com/openai/CLIP.git

# ── TF-IDF offline fallback ──────────────────────────────────────────────────
scikit-learn>=1.3.0

# ── Video clip extraction ─────────────────────────────────────────────────────
moviepy>=1.0.3

# ── Scene Description (BLIP-2 — downloads ~2GB model on first run) ────────────
transformers>=4.37.0
accelerate>=0.27.0

# ── Re-ID similarity search (FAISS) ───────────────────────────────────────────
faiss-cpu>=1.7.4             # swap to faiss-gpu if CUDA is available

# ── PDF Report Export ─────────────────────────────────────────────────────────
fpdf2>=2.7.6

# ── Already covered by the above — no extra installs needed for: ──────────────
#   anomaly_detector.py    (uses CLIP + numpy)
#   batch_processor.py     (uses existing modules)
#   spatial_filter.py      (uses numpy + opencv)
#   live_camera.py         (uses opencv + ultralytics)
#   exporter.py            (uses fpdf2 + json)
